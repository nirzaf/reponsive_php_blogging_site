image: samueldebruyn/debian-git

pipelines:
  default:
    - step:
        script:
          - apt-get update
          - apt-get install -qq git-ftp 
          - git ftp push --user $FTP_USERNAME --password $FTP_PASSWORD ftp://ftp.unaux.com/htdocs/ 
